package pluginsdk

// Version is the Go SDK release version. The CI release workflow reads this
// constant, verifies the module and tags go/vX.Y.Z when the version is new
// (see docs/releasing.md). Bump it to publish a new release.
const Version = "0.1.0"
